import React from "react";
import ReactDOM from "react-dom/client";
import Imagen from "./Imagen"; // Ensure correct path

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <Imagen />
  </React.StrictMode>
);
